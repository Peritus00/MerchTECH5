# ClamAV service for malware scanning
# Usage: docker-compose -f docker-compose.clamav.yml up -d
# Set CLAMAV_URL=tcp://localhost:3310 in your app environment

version: '3.8'

services:
  clamav:
    # Keep image current to avoid legacy 0.102.x engines that are blocked from updates.
    image: clamav/clamav:latest
    pull_policy: always
    container_name: merchtech-clamav
    ports:
      - "3310:3310"
    volumes:
      - clamav-data:/var/lib/clamav
    environment:
      - CLAMAV_NO_FRESHCLAM=false
    healthcheck:
      test: ["CMD", "clamdscan", "--ping"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 120s
    restart: unless-stopped

volumes:
  clamav-data:
